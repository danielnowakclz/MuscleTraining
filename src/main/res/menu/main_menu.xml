<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Navigation -->
    <item
        android:id="@+id/menu_training"
        android:title="Training" />

    <item
        android:id="@+id/menu_exercises"
        android:title="Übungen" />

    <!-- Divider -->
    <item
        android:id="@+id/divider_1"
        android:title="--"
        android:enabled="false" />

    <!-- Trainingsverwaltung -->
    <item
        android:id="@+id/menu_create_training"
        android:title="Training erstellen" />

    <item
        android:id="@+id/menu_delete_training"
        android:title="Training löschen" />

    <!-- Divider -->
    <item
        android:id="@+id/divider_2"
        android:title="--"
        android:enabled="false" />

    <!-- Aktionen -->
    <item
        android:id="@+id/menu_add_demo"
        android:title="Demo-Daten hinzufügen" />

    <item
        android:id="@+id/menu_delete_all"
        android:title="Alle Daten löschen" />

</menu>
